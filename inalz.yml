lang:
  source: en
  targets:
    - ja
documents:
  - source: kubectl/docs/book
    targets:
      ja: book
    locale: locale
options:
  lineIgnorePatterns:
    - "^\\s*{% \\w+ %}\\s*$"
    - '^{% sample lang="yaml" %}$'
